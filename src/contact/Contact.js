import React, { Component } from 'react'

class Contact extends Component{
  render(){
    return(
      <div>
        <div>Contact form, in dire need of styling!</div>
        <h1>It's a kind of magic that one has to do in order to successfully propagate the projectile into the offshore. xD</h1>
        <h2>Totally unrelated...</h2>
      </div>
    );
  };
}

export default Contact;
